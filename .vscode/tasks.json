{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Компилировать C",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${workspaceFolder}/main.c",
                "${workspaceFolder}/list/list.c",
                "${workspaceFolder}/hash_table/hash_table.c",
                "-o",
                "${workspaceFolder}/main"
            ],
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}